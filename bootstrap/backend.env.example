# backend.env.example
# Template de Variáveis de Ambiente - Backend
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para backend/.env
# 2. Substitua valores fictícios por valores reais (NÃO commite o .env)
# 3. Use valores de DESENVOLVIMENTO localmente (não use secrets de produção)
#
# AVISO CRÍTICO:
# - NUNCA commite arquivo .env com valores reais
# - NUNCA use secrets de produção localmente
# - NUNCA compartilhe este arquivo com valores reais

# ============================================================================
# DATABASE
# ============================================================================

# URL de conexão do PostgreSQL
# Formato: postgresql://user:password@host:port/database?sslmode=require
# Local: postgresql://postgres:password@localhost:5432/maternilove_dev
# Produção: Fornecido automaticamente pelo Railway
DATABASE_URL="postgresql://user:password@localhost:5432/maternilove_dev"

# ============================================================================
# AUTH (JWT)
# ============================================================================

# Secret para assinar Access Tokens (JWT)
# Obrigatório em produção
# Mínimo: 32 caracteres
# Como gerar: openssl rand -base64 32
# NUNCA use este valor em produção (é apenas exemplo)
JWT_SECRET="development-secret-change-in-production-minimum-32-chars"

# Secret para assinar Refresh Tokens (JWT)
# Opcional (usa JWT_SECRET se não fornecido)
# Recomendado: diferente de JWT_SECRET
# Como gerar: openssl rand -base64 32
# NUNCA use este valor em produção (é apenas exemplo)
JWT_REFRESH_SECRET="development-refresh-secret-change-in-production-minimum-32-chars"

# ============================================================================
# APP
# ============================================================================

# Ambiente de execução
# Valores: development | production
# Local: development
# Produção: production (configurado no Railway)
NODE_ENV="development"

# Porta onde servidor escuta
# Opcional (default: 3000)
# Local: 3000
# Produção: Fornecido automaticamente pelo Railway via $PORT
PORT=3000

# ============================================================================
# CORS
# ============================================================================

# Domínio permitido para requisições CORS
# Deve ser o domínio do frontend
# Local: http://localhost:3001 (ou porta que Next.js usar)
# Produção: https://maternilove-v2.vercel.app
CORS_ORIGIN="http://localhost:3001"

# ============================================================================
# FIM DO ARQUIVO
# ============================================================================
#
# Lembre-se:
# - Este é apenas um TEMPLATE
# - Copie para backend/.env e substitua valores fictícios
# - NUNCA commite backend/.env
# - Use valores de DESENVOLVIMENTO apenas
#
