# frontend.env.example
# Template de Variáveis de Ambiente - Frontend
#
# INSTRUÇÕES:
# 1. Copie este arquivo para frontend/.env.local
# 2. Substitua valores fictícios por valores reais (NÃO commite o .env.local)
# 3. Use valores de DESENVOLVIMENTO localmente (não use URLs de produção)
#
# AVISO CRÍTICO:
# - NUNCA commite arquivo .env.local com valores reais
# - Variáveis NEXT_PUBLIC_* são EXPOSTAS no bundle do cliente
# - NUNCA coloque secrets ou tokens em variáveis NEXT_PUBLIC_*

# ============================================================================
# API
# ============================================================================

# URL base do backend para requisições API
# AVISO: Variáveis NEXT_PUBLIC_* são EXPOSTAS no bundle do cliente
# Formato: http://host:port/api/v1 (local) ou https://host/api/v1 (produção)
# Local: http://localhost:3000/api/v1
# Produção: https://maternilove-v2-production.up.railway.app/api/v1
NEXT_PUBLIC_API_URL="http://localhost:3000/api/v1"

# ============================================================================
# AMBIENTE
# ============================================================================

# Ambiente de execução
# AVISO: Definido automaticamente pelo Next.js
# Não precisa configurar manualmente
# Valores: development | production
# NODE_ENV="development"

# ============================================================================
# FIM DO ARQUIVO
# ============================================================================
#
# Lembre-se:
# - Este é apenas um TEMPLATE
# - Copie para frontend/.env.local e substitua valores fictícios
# - NUNCA commite frontend/.env.local
# - Variáveis NEXT_PUBLIC_* são públicas (não coloque secrets)
#
